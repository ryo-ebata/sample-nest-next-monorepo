{
  "name": "sample-nest-next-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "description": "pnpm Monorepo with Next.js frontend and NestJS backend",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "turbo format",
    "check": "turbo check",
    "clean": "pnpm -r exec rm -rf node_modules dist .next .turbo",
    "biome:check": "biome check .",
    "biome:format": "biome format --write .",
    "biome:lint": "biome lint ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.0",
    "@types/node": "^20",
    "@vitest/ui": "^3.2.4",
    "c8": "^10.1.3",
    "turbo": "^2.0.0",
    "typescript": "^5",
    "vitest": "^3.2.4"
  }
}
